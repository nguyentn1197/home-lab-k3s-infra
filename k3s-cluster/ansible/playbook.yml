---
# ============================================================
# MAIN PLAYBOOK: Deploy K3s HA Cluster
# ============================================================

## Phase 1: Prepare all nodes
#- hosts: k3s_cluster
#  become: yes
#  roles:
#    - common
##
## Phase 2: Install masters (must be sequential — first master, then others)
#- hosts: masters
#  become: yes
#  serial: 1  # Run on one host at a time (order matters for cluster init!)
#  roles:
#    - k3s-master

# Phase 3: Install workers (can be parallel)
- hosts: workers
  become: yes
  roles:
    - k3s-worker
